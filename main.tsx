import React from 'react';
import ReactDOM from 'react-dom/client';
import GogginsDisciplineTracker from './GogginsDisciplineTracker';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GogginsDisciplineTracker />
  </React.StrictMode>
);
